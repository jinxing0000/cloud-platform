eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
spring:
  application:
    name: system-feign
  cloud:
    config:
      profile: dev
      label: master
      discovery:
        enabled: true
        serviceId: CONFIG-SERVER
    bus:
      enabled: true
      trace:
        enabled: true
  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest
  zipkin:
    base-url: http://localhost:9411
feign:
   hystrix:
       enabled: true