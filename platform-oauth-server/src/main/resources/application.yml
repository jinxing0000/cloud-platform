server:
  port: 8766
spring:
  application:
    name: oauth-server
  redis:
    host: 127.0.0.1
    port: 6379
    password:
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
  instance:
    prefer-ip-address: true